[build]
  publish = "."
  command = "npm install"

[functions]
  directory = "netlify/functions"

# Otomatik haber ve piyasa tarama workflow'u (Her saat başı çalışır)
[functions."sync-bot"]
  schedule = "@hourly"
